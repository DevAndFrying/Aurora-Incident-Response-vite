{
  "name": "aurora-incident-response",
  "version": "0.7.0",
  "description": "Local-first incident response case tracking application",
  "author": "Mathias Fuchs and Aurora contributors",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "test": "node browser-storage.test.mjs",
    "check": "node --check browser-storage.mjs && node --check renderer.mjs && npm test && vite build"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "license": "Apache-2.0"
}
